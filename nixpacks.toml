# Added by Hermes migration (2026-08-04)
# Astro app -> needs Node >=22.12.0; nixpacks Node 22 = 22.11 (too old) -> use 23.
[phases.install]
commands = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[variables]
NIXPACKS_NODE_VERSION = "23"
