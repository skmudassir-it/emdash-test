# Added by Hermes migration (2026-08-04)
# Astro app -> needs Node >=22.12.0. nixpacks v1.41 supports 18/20/22/24 (NOT 23).
[phases.install]
commands = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[variables]
NIXPACKS_NODE_VERSION = "24"
