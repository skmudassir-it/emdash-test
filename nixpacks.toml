# Added by Hermes migration (2026-08-04)
# Repo has pnpm-lock.yaml but no workspace packages -> pnpm build breaks.
# Force npm for install + build.
[phases.install]
commands = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[variables]
NIXPACKS_NODE_VERSION = "20"
