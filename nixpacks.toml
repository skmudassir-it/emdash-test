# Added by Hermes migration (2026-08-04)
# Astro app -> needs Node >=22.12. Repo has pnpm-lock without workspace -> force npm.
[phases.install]
commands = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[variables]
NIXPACKS_NODE_VERSION = "22"
